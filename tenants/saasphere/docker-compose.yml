services:
  web:
    image: nginx:alpine
    container_name: futuraweb
    ports:
      - "8083:80"
    volumes:
      - .:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
